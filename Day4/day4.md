# File Handling in Python – Task Summary

## Date: 28 July 2025

### Tasks Performed

1. **Learned basic file operations:**
   - Used `open()` function with different modes: `"r"`, `"w"`, `"a"`, `"x"`.
   - Practiced file writing and reading.
   - Used `with` statement to automatically close files.

2. **Error Handling in File Operations:**
   - Implemented `try`, `except` blocks to catch file-related errors like `FileNotFoundError`.

3. **Created a sample file:**
   - Wrote `"Hello World"` into `sample.txt` using write mode.

4. **Built a Simple To-Do List Project:**
   - Created `todo.txt` to store tasks.
   - Added tasks using `append` mode.
   - Viewed tasks by reading the file content.
   - Used conditional logic and input to build a basic menu system.

5. **Step-by-step improvements:**
   - Enhanced project in phases for better structure and usability.

---

### Files Used:
- `sample.txt`
- `todo.txt`

### Concepts Covered:
- File creation
- Writing and appending to files
- Reading file contents
- Error handling with `try-except`
- Working with `os.path.exists()`
- Menu-based interaction using `input()` and `if-else` conditions
